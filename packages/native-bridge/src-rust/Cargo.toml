[package]
name = "sous-native-bridge"
version = "0.1.0"
edition = "2021"

[dependencies]
tauri = { version = "2.0.0-rc", features = [] }
serde = { version = "1.0", features = ["derive"] }
sysinfo = "0.30"

[lib]
name = "sous_native_bridge"
crate-type = ["lib", "cdylib", "staticlib"]
