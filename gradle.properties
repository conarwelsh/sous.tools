# Set the Java Home for the WSL environment
# This path MUST match the location of your OpenJDK 17 inside WSL
org.gradle.java.home=/usr/lib/jvm/java-17-openjdk-amd64
# Disable configuration cache if it's causing issues with composite builds
org.gradle.configuration-cache=false